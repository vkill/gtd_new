%header.container_12.clearfix
  .grid_12
    %h1= t("web.page.index", :model => @model.model_name.human)
%section.container_12.clearfix
  .grid_12
    .actions
      = link_to t("web.page.new", :model => @model.model_name.human), [:new, :admin, @item_symbol], :class => "button"
    #demo.clearfix
      = render "entries", :items => @items

= content_for :javascript do

  :javascript
    $(document).ready(function() {
        $('.list_table').dataTable( {
            "sPaginationType": "full_numbers",
            "aaSorting": [[0,'asc']],
            "oLanguage": {
              "sUrl": "/javascripts/datatables/zh-CN.txt"
            }
        });
    });


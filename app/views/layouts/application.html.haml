!!!
%html{:xmlns => "http://www.w3.org/1999/xhtml"}
  %head
    %meta{"http-equiv" => "Content-Type", :content => "text/html; charset=utf-8"}
    %meta{:name => "keywords", :content => "四川职业建筑技术学院,图书信息中心,故障保修,业务办理"}
    = csrf_meta_tag

    %title 四川职业建筑技术学院,图书信息中心,故障保修,业务办理
    = favicon_link_tag

    = stylesheet_link_tag 'design', :media => 'screen'
    - if ['users', 'passwords', 'sessions'].index params[:controller]
      = stylesheet_link_tag 'simple_form', :media => 'screen'
      = stylesheet_link_tag 'client-side-validations', :media => 'screen'
    = yield :head


  %body
    = render "shared/header"
    - unless ['users', 'passwords', 'sessions'].index params[:controller]
      = render "shared/banner"

    #content
      - flash.each do |type, message|
        %div
          %p.msg{:class => "type"}
            = message

      = yield

      .clear

    = render "shared/footer"
    = render "shared/javascripts"


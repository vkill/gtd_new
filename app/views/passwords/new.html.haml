#log_form
  = form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :class => "form2" }) do |f|
    #form_bg_top
    #form_bg_mid
      #form
        = render "shared/error_messages", :target => resource
        = raw("邮&nbsp&nbsp&nbsp&nbsp箱：")
        = f.email_field :email, :id => "username"
        %br
        = display_custom_captcha :template => :mycaptcha
        %br
        = f.submit :id => "submit_bt", :type => "image", :src => image_path("design/submit_buttun_2.jpg")
        %br
    #form_bg_foot


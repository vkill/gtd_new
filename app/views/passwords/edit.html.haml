#log_form
  = form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :class => "form2", :method => :put }) do |f|
    #form_bg_top
    #form_bg_mid
      #form
        = render "shared/error_messages", :target => resource
        = f.hidden_field :reset_password_token
        = raw("密&nbsp&nbsp&nbsp&nbsp码：")
        = f.password_field :password, :id => "psw"
        %br
        = raw("确认密码：")
        = f.password_field :password_confirmation, :id => "psw"
        %br
        = f.submit :id => "submit_bt", :type => "image", :src => image_path("design/submit_buttun_2.jpg")
        %br
    #form_bg_foot

